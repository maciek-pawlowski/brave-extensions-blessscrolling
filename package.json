{
  "name": "blessscrolling",
  "version": "0.2.3",
  "private": true,
  "description": "Local Manifest V3 extension for filtering Shorts/Reels in Brave.",
  "scripts": {
    "test": "node tests/matcher.test.js",
    "validate": "node -e \"JSON.parse(require('fs').readFileSync('manifest.json', 'utf8')); console.log('manifest ok')\""
  }
}
